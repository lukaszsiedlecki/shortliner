services:
  shortliner:
    build: .
    container_name: shortliner-app
    ports:
      - "${SERVER_PORT}:${SERVER_PORT}"
    env_file:
      - .env.local
    networks:
      - shortliner-net

networks:
  shortliner-net:
    external: true
